on: [push]

jobs:
  ApkSizeTracker:
    runs-on: ubuntu-latest
    name: Apk size tracker
    steps:
      # To use this repository's private action,
      # you must check out the repository
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 16
          
      - name: Custom action step
        id: apkSize
        uses: zsfVishnu/apk-size-tracker@master # Uses an action in the root directory
        with:
          flavor: 'debug'
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#       - run: echo {{ steps.apkSize.outputs }} > myArtifact.txt
#       - uses: actions/upload-artifact@v3
#         with:
#           name: my-artifact
#           path: myArtifact.txt